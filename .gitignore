# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.iml

# OS / Windows alternate data streams (WSL2)
*:Zone.Identifier
*.Zone.Identifier
Thumbs.db
.DS_Store

# ScanFlow runtime
~/.scanflow_session.json
~/.scanflow/

# Lab-PC logs pulled by tools/lab-sync/pull_logs.bat — diagnostic only,
# not source-of-truth, can accumulate to many MB. Keep the two original
# probe logs that document the SCAN.OFFSET.X.NM finding (already in the
# repo); ignore anything new.
/lab-logs/scanflow_*.log
!/lab-logs/scanflow_20260520*.log
